import streamlit as st

# --- НАСТРОЙКИ СТРАНИЦЫ ---
st.set_page_config(
    page_title="Алгебра: Формулы",
    page_icon="📐",
    layout="wide",
    initial_sidebar_state="expanded"
)

# --- СОВРЕМЕННЫЙ СТИЛЬ (CSS) ---
st.markdown("""
    <style>
    /* Основной фон и шрифты */
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');
    
    html, body, [class*="css"] {
        font-family: 'Inter', sans-serif;
    }

    /* Оформление карточки теории */
    .theory-card {
        background-color: #ffffff;
        padding: 25px;
        border-radius: 15px;
        border: 1px solid #e1e4e8;
        box-shadow: 0 4px 6px rgba(0,0,0,0.02);
        margin-bottom: 20px;
        color: #1a1c23;
    }

    /* Оформление примера */
    .example-card {
        background-color: #f8faff;
        padding: 20px;
        border-radius: 12px;
        border-left: 5px solid #4a90e2;
        margin-top: 15px;
    }

    /* Убираем лишние отступы в заголовках */
    .stMarkdown h1 { padding-top: 0; }
    
    /* Кастомное меню в сайдбаре */
    .stRadio > label {
        font-weight: 600;
        font-size: 1.1rem;
        color: #4a5568;
    }
    </style>
""", unsafe_allow_html=True)

# --- БАЗА ДАННЫХ КОНТЕНТА (1-10) ---
CONTENT = {
    "1. Уравнения прямой (Общее и Каноническое)": {
        "theory": r"""
        ### 1. Общее уравнение прямой
        Любая прямая на плоскости описывается уравнением первой степени:
        $$Ax + By + C = 0$$
        Где вектор $\vec{n} = (A, B)$ является **нормальным вектором** (перпендикулярен прямой).

        ### 2. Каноническое уравнение
        Если прямая проходит через точку $M_0(x_0, y_0)$ параллельно направляющему вектору $\vec{a} = (p, q)$:
        $$\frac{x - x_0}{p} = \frac{y - y_0}{q}$$
        """,
        "example": r"**Задача:** Составить уравнение прямой через точку $M(2, -1)$ с нормалью $\vec{n}(3, 4)$.  "
                   r"**Решение:** Используем формулу $A(x-x_0) + B(y-y_0) = 0$:  "
                   r"$3(x-2) + 4(y+1) = 0 \Rightarrow 3x + 4y - 2 = 0$.",
        "image": "https://upload.wikimedia.org/wikipedia/commons/thumb/0/0e/Linear_function_graph.svg/300px-Linear_function_graph.svg.png"
    },
    "2. Уравнение в отрезках и Параметрическое": {
        "theory": r"""
        ### 1. Уравнение в отрезках
        Если прямая пересекает оси координат в точках $(a, 0)$ и $(0, b)$:
        $$\frac{x}{a} + \frac{y}{b} = 1$$
        Где $a$ и $b$ — величины отрезков, отсекаемых на осях $OX$ и $OY$.

        ### 2. Параметрическое уравнение
        Выражается через параметр $t \in \mathbb{R}$:
        $$\begin{cases} x = x_0 + pt \\ y = y_0 + qt \end{cases}$$
        """,
        "example": r"**Задача:** Прямая задана как $\frac{x}{3} + \frac{y}{-2} = 1$. Найти точки пересечения.  "
                   r"**Решение:** Прямая пересекает ось $X$ в точке $(3, 0)$ и ось $Y$ в точке $(0, -2)$.",
        "image": None
    },
    "3. Расстояние от точки до прямой. Угол": {
        "theory": r"""
        ### 1. Расстояние от точки до прямой
        Расстояние $d$ от точки $M_0(x_0, y_0)$ до прямой $Ax+By+C=0$:
        $$d = \frac{|Ax_0 + By_0 + C|}{\sqrt{A^2 + B^2}}$$

        ### 2. Угол между двумя прямыми
        Если даны прямые с угловыми коэффициентами $k_1$ и $k_2$:
        $$\tan \phi = \left| \frac{k_2 - k_1}{1 + k_1 k_2} \right|$$
        """,
        "example": r"**Задача:** Найти расстояние от точки $(0,0)$ до прямой $3x + 4y - 10 = 0$.  "
                   r"**Решение:** $d = \frac{|3\cdot0 + 4\cdot0 - 10|}{\sqrt{3^2 + 4^2}} = \frac{10}{5} = 2$.",
        "image": None
    },
    "4. Уравнение плоскости в отрезках": {
        "theory": r"""
        Аналогично прямой, плоскость, пересекающая оси координат в точках $(a, 0, 0)$, $(0, b, 0)$ и $(0, 0, c)$, задается уравнением:
        $$\frac{x}{a} + \frac{y}{b} + \frac{z}{c} = 1$$
        Где $a, b, c \neq 0$.
        """,
        "example": r"Если плоскость задана уравнением $2x + 3y + 6z - 12 = 0$, разделим всё на 12:  "
                   r"$\frac{x}{6} + \frac{y}{4} + \frac{z}{2} = 1$. Отрезки: $a=6, b=4, c=2$.",
        "image": None
    },
    "5. Параллельность и перпендикулярность": {
        "theory": r"""
        Для двух прямых $L_1: A_1x + B_1y + C_1 = 0$ и $L_2: A_2x + B_2y + C_2 = 0$:

        1. **Условие параллельности:** Направляющие векторы коллинеарны, а нормали пропорциональны:
        $$\frac{A_1}{A_2} = \frac{B_1}{B_2} \neq \frac{C_1}{C_2}$$

        2. **Условие перпендикулярности:** Скалярное произведение нормалей равно нулю:
        $$A_1 A_2 + B_1 B_2 = 0$$
        """,
        "example": r"Прямые $2x + 3y - 5 = 0$ и $4x + 6y + 10 = 0$ параллельны, так как $\frac{2}{4} = \frac{3}{6}$.",
        "image": None
    },
    "6. Общее уравнение плоскости. Расстояние": {
        "theory": r"""
        ### 1. Общее уравнение плоскости
        $$Ax + By + Cz + D = 0$$
        Вектор $\vec{n}(A, B, C)$ — нормаль к плоскости.

        ### 2. Расстояние от точки до плоскости
        Для точки $M_0(x_0, y_0, z_0)$:
        $$d = \frac{|Ax_0 + By_0 + Cz_0 + D|}{\sqrt{A^2 + B^2 + C^2}}$$
        """,
        "example": r"**Задача:** Найти нормаль плоскости $z = 2x - 5y + 3$.  "
                   r"**Решение:** Перенесем всё в одну сторону: $2x - 5y - z + 3 = 0$. Нормаль $\vec{n} = (2, -5, -1)$.",
        "image": None
    },
    "7. Уравнение окружности и сферы": {
        "theory": r"""
        ### 1. Окружность (на плоскости)
        С центром в $(x_0, y_0)$ и радиусом $R$:
        $$(x - x_0)^2 + (y - y_0)^2 = R^2$$

        ### 2. Сфера (в пространстве)
        С центром в $(x_0, y_0, z_0)$ и радиусом $R$:
        $$(x - x_0)^2 + (y - y_0)^2 + (z - z_0)^2 = R^2$$
        """,
        "example": r"Уравнение $x^2 + y^2 = 9$ задает окружность с центром в начале координат и радиусом 3.",
        "image": None
    },
    "8. Векторы: Сложение и Скаляр": {
        "theory": r"""
        ### 1. Сложение векторов
        Если $\vec{a} = (a_1, a_2)$ и $\vec{b} = (b_1, b_2)$, то:
        $$\vec{a} + \vec{b} = (a_1 + b_1, a_2 + b_2)$$
        (Правило треугольника или параллелограмма).

        ### 2. Умножение на скаляр $\alpha$
        $$\alpha \vec{a} = (\alpha a_1, \alpha a_2)$$
        """,
        "example": r"$\vec{a}(1, 2) + \vec{b}(3, -4) = \vec{c}(4, -2)$. Длина вектора $|\vec{a}| = \sqrt{1^2 + 2^2} = \sqrt{5}$.",
        "image": None
    },
    "9. Скалярное произведение векторов": {
        "theory": r"""
        ### 1. Геометрическое определение
        $$\vec{a} \cdot \vec{b} = |\vec{a}| \cdot |\vec{b}| \cdot \cos \phi$$

        ### 2. В координатах
        Для $\vec{a}(a_1, a_2, a_3)$ и $\vec{b}(b_1, b_2, b_3)$:
        $$\vec{a} \cdot \vec{b} = a_1 b_1 + a_2 b_2 + a_3 b_3$$
        """,
        "example": r"Найти скалярное произведение $\vec{a}(1, 3, -5)$ и $\vec{b}(4, -2, -1)$.  "
                   r"**Решение:** $1\cdot4 + 3\cdot(-2) + (-5)\cdot(-1) = 4 - 6 + 5 = 3$.",
        "image": None
    },
    "10. Формула угла между векторами": {
        "theory": r"""
        Косинус угла $\phi$ между векторами вычисляется как отношение их скалярного произведения к произведению их длин:
        $$\cos \phi = \frac{\vec{a} \cdot \vec{b}}{|\vec{a}| \cdot |\vec{b}|}$$
        $$\cos \phi = \frac{a_1 b_1 + a_2 b_2 + a_3 b_3}{\sqrt{a_1^2 + a_2^2 + a_3^2} \cdot \sqrt{b_1^2 + b_2^2 + b_3^2}}$$
        """,
        "example": r"Если $\vec{a} \cdot \vec{b} = 0$, то $\cos \phi = 0$, следовательно векторы ортогональны ($\phi = 90^\circ$).",
        "image": None
    }
}

# --- САЙДБАР (НАВИГАЦИЯ) ---
st.sidebar.title("📚 Алгебра")
st.sidebar.markdown("Список экзаменационных вопросов:")
selection = st.sidebar.radio(
    label="Перейти к вопросу:",
    options=list(CONTENT.keys()),
    label_visibility="collapsed"
)

# --- ОСНОВНАЯ ЧАСТЬ ---
st.header(selection)

data = CONTENT[selection]

# Рендеринг Теории
st.markdown('<div class="theory-card">', unsafe_allow_html=True)
st.markdown(data["theory"])
st.markdown('</div>', unsafe_allow_html=True)

# Изображение (если есть)
if data["image"]:
    st.image(data["image"], caption="Визуализация к теме", width=400)

# Рендеринг Примера
st.markdown("#### 💡 Пример решения")
st.markdown('<div class="example-card">', unsafe_allow_html=True)
st.markdown(data["example"])
st.markdown('</div>', unsafe_allow_html=True)

# Кнопки "Назад" / "Вперед" (Опционально для iPad)
col1, col2 = st.columns(2)
with col1:
    st.button("⬅️ Предыдущий вопрос", use_container_width=True, disabled=True if selection == list(CONTENT.keys())[0] else False)
with col2:
    st.button("Следующий вопрос ➡️", use_container_width=True, disabled=True if selection == list(CONTENT.keys())[-1] else False)

# --- ФУТЕР ---
st.sidebar.markdown("---")
st.sidebar.caption("Подготовлено для iPad. Python + Streamlit + LaTeX")
